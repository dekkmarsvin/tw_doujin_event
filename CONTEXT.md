# 領域詞彙

本專案的詞彙表。文件與程式碼命名以此為準；同義詞在括號中標為「避免」的說法。

先看最容易混淆的四組。

## 容易混淆的詞

### 「登入」有兩種，方向相反

| 說法 | 意思 | 狀態 |
|---|---|---|
| **社團登入** | 參展社團在 `/circle` 以 email 一次性連結登入，維護**自己的**公開資料 | 已上線 |
| **授權外部服務** | 使用者授權 pixiv 等服務，以便把外部內容**拉進**本站 | P2，未實作 |

前者由當事人在本站填自己的資料，不涉及任何外部服務；後者把外部資料匯入本站。兩者不共用任何機制。**一般參觀者永遠不需要登入。**

### 「社團」與「攤位」不是一對一

- 一個 **`CircleRecord`**（社團）可以有多筆 **`PlacementRecord`**（配置）——跨日或連號攤位。
- 一個 **攤位**（booth）在不同日可能有不同社團。
- 收藏、備註與行程掛在 `CircleRecord.id` 上，**不掛在攤位上**。

### 「場館」與「展區」不是同一件事

| 說法 | 意思 | FF47 的值 |
|---|---|---|
| **場館**（venue） | 活動實際舉辦的建築。由 `EventDefinition.venue` 提供 | 花博公園爭豔館 |
| **展區**（area） | 場館內部的分區代碼，供篩選與定位使用 | `ALL`（全館）、`A`（A–K 區）、`B`（L–W 區） |

**場館名稱屬於活動資料，不寫死在程式或文件裡。** 換一場活動就換一個場館；文件要提到場館時說「活動定義的場館」，不複製當期的名稱。

展區是同一個場館的切分。FF47 的 `A` 與 `B` 是爭豔館的兩半，**不是兩個場館**——所以 `areaMode` 是 `single`，介面不顯示切換。

**`Booth["hall"]` 的名字說謊**：它存的是展區代碼（`"A" | "B"`），不是場館。名稱是歷史遺留，且已寫進公開快照 schema，改名不是改一個識別字。**文件與新程式碼一律用「展區」／`area`。**

### 「地圖」有三層意思

| 說法 | 指什麼 |
|---|---|
| **配置圖** | 主辦提供的原始 JPG。只在 authoring 階段用來辨識與對照 |
| **layout** | 辨識後的向量資料（排、slot、柱子、出入口） |
| **社團攤位配置圖** | 使用者可見的名稱。「向量地圖」是實作用語，**不對使用者說** |

---

## 資料

**場刊**（catalog）
本站呈現的社團與攤位資料整體。對應公開快照 `circles.json`。

**社團模板**（circle template）
社團身分由永久配號 registry 與可審閱證據決定。活動中的正式證據是主辦攤位鍵（`eventId + day:booth`）；生成器把它與主辦當日攤位清單接起來，產生 `CircleRecord` 與 `PlacementRecord`。

**快照**（snapshot）
版本控制中的靜態 JSON 產物，是公開資料的唯一真相。`circles.json`（場刊）與 `map.json`（地圖）。避免說「靜態檔」。

**補充資料 / overlay**（override）
社團自己在 `/circle` 填寫、疊加在快照之上的內容。可即時撤下，一律標示為「社團自述／尚未驗證」。避免說「社團資料」——那會和 `CircleRecord` 混淆。

**來源**（`SourceLink`）
一筆內容的出處：提供者、內容類型、原始連結、擷取時間與狀態。現行生產者是 `official`（主辦）與 `circle`（社團本人）；`catalog`、`social`、`media` 只保留為歷史／連結分類，不抓取第三方內容。**只有主辦來源可標示「主辦來源」；其他一律不得使用「官方」措辭。**

**facet**
資料生成階段已正規化的搜尋欄位（`circleCategory`、`creatorTypes`、`referencedWorks`、`workTypes`、`ageRatings`）。`circleCategory` 只能選自活動定義所固定的主辦分類目錄；其他 facet 仍是社團自由填寫。UI 只消費，不再切割字串。

**revision**
地圖 layout 的版本號，每次發布遞增。

## 規劃

以下四者是**獨立狀態**，互不隱性改寫：

**收藏**（`FavoriteRecord`）
記錄存在即代表已收藏。含群組、備註與時間。

**收藏群組**（`FavoriteGroup`）
使用者自訂的收藏分類，`name` 必填。顏色只作輔助識別，**不得成為唯一分類依據**。

**行程**（`VisitPlanEntry`）
特定活動日的走訪計畫，狀態為 `planned` / `next` / `visited`，另含順序、購買項目與預算。

**下一站**（next）
每個活動日最多一筆。只有明確的「設為下一站」動作能建立或轉移它。

**導航模式**
把地圖暫時投影為當日行程的檢視，可隨時退出。不是一種模式狀態，不清空使用者原本的搜尋與篩選。

**規劃資料**
收藏、群組、備註、行程、購買項目與預算的總稱。只存在使用者當下的瀏覽器。

## 社團控制面

**控制面**（control plane）
與閱讀端分離的受驗證管理介面。目前只有一個：社團自助控制面 `/circle`。地圖 authoring 尚未拆成控制面。

**認領**（claim）
社團證明自己是某個 `CircleRecord` 的擁有者的流程。email 只證明控制信箱，認領必須另有證據。

**撤下**（takedown）
管理者即時移除某筆社團補充資料，該筆立刻自公開文件消失。

**活動後退出**（post-event opt-out）
社團選擇自己填寫的補充資料在活動結束後不再公開。範圍**只限社團自述內容**，主辦公布的社團名、攤位與日期不受影響。

## 地圖

**slot**
一格攤位，保存矩形座標。互動元素是 slot 本身，不是圖片上的座標點。

**排**（row）
A–W。A–V 縱向，W 橫向。

**landmark**
非一般攤位區（企業攤、舞台等），保存相對矩形，不自動辨識。

**authoring**
受信任維護者在本機辨識、微調並發布地圖 layout 的工作。**不是產品功能**，不出現在公開 Pages。

## 工程

**gate**
交付前必須全數通過的檢查：`npm test`、`npm run lint`、`npx tsc --noEmit`。CI 跑同一組。

**產物邊界**（artifact boundary）
公開 build 中什麼必須存在、什麼絕不能存在的規則。由測試把關，不靠人工檢查。

**分期**（P0 / P1 / P2）
P0 是核心流程，P1 是完善，P2 是尚未開放的能力。**P2 不等於待辦**——它是明確被推遲的範圍，解除需要對應的 ADR。
